> 历史记录：本文描述 0.2.0 C++ 版本。当前产品已迁移到 Rust，请从 README.md 和 RUST.md 开始。

# 原生实现说明

目标是鼠标只有一个可见指针，静止时没有定频采样，分发无需托管桌面运行时。当前实现使用本机已有的 MSVC C++20 和 Win32；设置窗口直接使用系统控件。没有引入 Qt、WebView、Electron、服务、驱动或第三方运行库框架。

**语言和体系的取舍**

Rust 配合 Win32 也适合这一目标。Rust 不需要垃圾回收器，所有权模型有助于管理资源和并发；微软维护的 [windows-rs](https://github.com/microsoft/windows-rs) 提供 Windows API 绑定。对于长期维护的新项目，Rust 值得考虑，代价是引入工具链、FFI 和窗口回调边界的封装。Win32 调用仍需遵守句柄所有权、线程和系统状态约束，改用 Rust 不会自动解决这些问题。参考 [Rust 官方介绍](https://rust-lang.org/)。

C++ / Win32 同样能直接编译为机器码，现有 Windows 头文件、资源编译器和调试工具可直接使用。本次先完成这一实现，获得实际行为和占用数据。没有进行 C++ 与 Rust 的同功能基准比较，也不声称其中一种在这个工具上天然更快。Zig 也能直接调用 Win32，但这个项目尚无证据说明再换语言能比减少唤醒、去除覆盖层和桌面框架带来更多收益。

**输入与动画**

`native/core.h` 不依赖 Windows，包含固定容量轨迹识别、动画包络和边缘方向选择。识别检查时间窗口、至少三次有效方向反转、移动幅度、累计路程、净位移与往返比值。触发后消耗已用证据，最短重复触发间隔 220 毫秒。鼠标按键按下清空轨迹，松开后冷却 180 毫秒。代表性测试位于 `native/core-tests.cpp`。

`native/main.cpp` 用 `RIDEV_INPUTSINK` 注册鼠标 Raw Input，只接收副本；不使用 `RIDEV_NOLEGACY`，不吞掉按键、滚轮或拖拽消息。鼠标事件到达后，最多每 8 毫秒采集一次真实物理位置，按目标显示器 DPI 转换位移供识别。事件路径不创建集合。高回报率设备的每条输入消息仍需分发，1000 / 8000 Hz 物理设备负载尚需测试。

闲置时主程序阻塞在消息及恢复进程句柄等待中。只有效果激活时运行 15 毫秒窗口计时器，实际频率受调度影响，不调用 `timeBeginPeriod`。放大约 150 毫秒，缩小约 260 毫秒；重复触发从当前大小继续。停留期间没有尺寸、方向或指针角色变化就不更新位图，Windows 负责移动真实指针。结束后 3 秒的一次性计时器释放光标帧缓存；之后无周期计时器。

`native/cursor.h` 生成超采样、预乘 Alpha 的黑白箭头。缓存最多 32 个独立 HCURSOR，超出后回收最近最少使用的帧。每次传给 `SetSystemCursor` 的都是副本，因为该 API 会销毁输入句柄。只替换当时正在显示的标准指针角色，切换到其他标准角色时随动画更新；恢复时重新加载完整系统指针方案。应用自行创建的私有指针不会被替换。

**DPI、热点和屏幕边缘**

EXE 清单使用 PerMonitorV2。隐藏的 1 像素 DPI 窗口移到鼠标所在显示器，获取该屏 DPI；效果绘制和热点使用物理像素。内置箭头参考高度为 35 DIP，因此“4 倍”指内置箭头的倍率，不是精确复制当前辅助功能指针大小后乘四。

单帧最大高度 256 物理像素；高倍率、高缩放时会被截到该上限。右侧、底部边缘镜像整个箭头及其热点，让箭身朝向屏内。方向由最大尺寸和滞回决定，避免动画缩放时反复翻转。Windows 最终呈现仍可能受显示驱动的光标平面限制影响，不能保证每种硬件都使用硬件光标平面。

原生方案不创建透明覆盖层，也不调用 `ShowCursor` 隐藏真实指针。热点固定在尖端白色描边处，鼠标移动和点击位置由系统管理。光标不可见、受触控抑制或查询失败时结束效果；任意按键开始也结束效果。

**恢复机制及边界**

`native/guardian.h` 实现独立恢复进程，与主进程运行同一 EXE。主程序必须收到恢复进程的就绪信号才可修改光标。恢复进程通过受限继承句柄得到父进程句柄、共享状态、事件，不用命名管道。

- 第一次修改前，先写入并刷新 `native-recovery.flag`，再标记共享状态为已修改。恢复进程此时每 250 毫秒检查一次心跳；闲置时无限期等待内核事件。
- 正常效果结束、暂停、点击、打开设置、锁屏、挂起或退出，都调用 `SPI_SETCURSORS` 重新加载用户配置的指针方案，成功后清除恢复标记。
- 主进程意外结束，恢复进程检测到进程句柄信号并恢复指针。
- 动画期间心跳超过 1.5 秒未更新，恢复进程先结束自己的父进程，再恢复指针，防止迟到的动画帧重新覆盖恢复结果。
- 恢复进程单独退出，主进程在消息等待中立即得知，恢复指针并禁用效果。需重启程序重新创建保护。
- 两个进程同时被结束、整个进程树被杀死或系统级故障，无法承诺后台自动恢复。恢复标记用于下次启动修复；`--restore` 可手动重新加载指针方案。这个失败边界可能留下放大的指针，程序从未将系统指针设置为透明，也未修改输入映射。

不写入系统光标主题注册表，不永久修改方案。`SPI_SETCURSORS` 恢复的是用户配置的方案；其他工具同时做的临时替换可能被覆盖。辅助功能彩色/特大指针、第三方动态主题仍需人工检查恢复效果，不应同时运行另一个系统光标管理工具。

**设置、控制和代码位置**

配置为 `%LOCALAPPDATA%\ShakeSpot\native-settings.ini`，保存通过同目录临时文件替换。解析无效字段时保留默认值，数值越界会钳制。与旧覆盖层版本的 JSON 分开。错误记录为同目录 `native-error.log`，不记录轨迹坐标或按键内容。

开机启动默认关闭，保存勾选后仅修改当前用户 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 的 `ShakeSpotNative` 项。移动 EXE 后应重新关闭并开启此选项。任务管理器禁用启动项可能覆盖该设置；重新登录尚需手工验收。

支持 `--settings`、`--pause`、`--resume`、`--preview`、`--exit`、`--restore`。控制参数作用于当前会话的唯一实例。没有实例时，除 `--settings` 和 `--restore` 外返回退出码 2。`--data-dir <目录>` 可隔离配置，但同一会话仍只允许运行一个原生实例，因为系统光标状态是共享的。

`--test-mode` 只用于桌面测试，允许模拟主线程卡死，并禁止测试设置写入真实开机启动项。产品正常运行不会合成输入；`SendInput`、暂停测试进程等只在 `desktop-tests.cpp` 中使用。

主要文件：`core.h` 识别和动画；`main.cpp` 输入、托盘、设置和消息循环；`cursor.h` 光标资源；`guardian.h` 故障恢复；`settings.h` 配置；`resources.rc` 原生界面；`scripts/native.ps1` 构建和发布。

