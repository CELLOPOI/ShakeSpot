# Rust 实现说明

当前产品版本为 0.4.1。主程序、恢复进程、输入识别、动画、位图生成、托盘、设置与开机启动均由 Rust 实现，入口为 `rust/main.rs`。Windows API 使用微软的 `windows-sys 0.61.2`，传递依赖为 `windows-link 0.2.1`，由 `Cargo.lock` 固定。产品构建不会编译或链接历史 C++ 应用。原生设置资源由 Windows SDK 的 `rc.exe` 编译；黑盒桌面测试驱动保留 C++，这样可以独立检查 Rust 程序的实际行为。

本机使用 Rust 1.93.1、Cargo 1.93.1、x86_64-pc-windows-msvc，复用 Visual Studio 2022 Build Tools 与 Windows SDK。Release 使用优化等级 3、ThinLTO、一个 codegen unit、静态 CRT，以及 `panic=abort`。工具链配置跟随 stable；复现实测时应核对报告中的编译器版本。未安装 Rust 的机器可参考 [官方安装说明](https://rust-lang.org/tools/install/)。优先使用已有的 rustup 安装。可选的 `scripts/bootstrap-rust.ps1` 遵循已有 `CARGO_HOME` / `RUSTUP_HOME`，未配置时使用当前用户的 `.cargo` / `.rustup`；也可通过参数指定目录。该脚本会安装工具链并更新当前用户的工具环境变量，构建本身不需要反复运行它。

**事件与性能**

静止时主程序等待 Windows 消息和恢复进程句柄，没有周期位置轮询。Raw Input 使用 `RIDEV_INPUTSINK` 接收原始移动量，按键按下事件优先处理。相对移动累加设备计数；绝对输入先保存首个位置，再按主屏或虚拟桌面的范围与当前 DPI 换算位移。设备、坐标模式切换及长输入间断会清空旧轨迹。

相对输入按每 4 个设备计数换算 1 个识别单位；默认有效反向幅度为 96 个计数，最小范围为 208 个计数，累计路程为 840 个计数。此映射是初始阈值，不等于物理距离，不保证不同硬件 DPI 的手感一致。Windows 指针速度和屏幕裁切不再决定相对识别轨迹。屏幕物理位置保留用于可见性、所在显示器、边缘朝向和绘制。

常规样本间隔为 8 毫秒；间隔内遇到足够幅度的反向会先补入转折点，避免来回运动抵消。回调队列只合并同设备、同坐标模式、同分量方向且跨度不超过 8 毫秒的相对移动，保留按键和反向证据。采样轨迹固定为 96 点；转折补样不承诺所有输入下严格不超过 125 次每秒。

0.4.1 在完整判定前增加必要条件预筛。环形窗口增减样本时维护 `Σ(|dx| + |dy|)`，它是二维路程及任意单位主轴投影路程的上界；首尾点最大单轴位移则是二维净位移的下界。上界尚不足最小路程/速度要求，或净位移下界已超过上界的 42% 时，直接返回。每绕回环形缓冲区重新求和，并保留舍入余量，限制长期累计误差。

候选轨迹继续使用 0.4.0 的完整判定。预筛增加 8 字节状态，没有缩减采样或改为仅在刚转向时检查，因此仍在反向幅度达到门槛的那个样本确认。逐样本差分基准冻结在 `tests/support/detector_v040.rs`，不参与产品构建。

完整判定计算二维路程、包围盒对角线与净位移：路程/对角线至少 2.4，净位移/路程不超过 0.42。随后通过轨迹的主要方向投影计算有效反向，保留至少 3 次反向、幅度、累计路程、速度、最小时间跨度和冷却限制。这样减少固定 X/Y 轴造成的斜向差异，也过滤另一方向持续大幅漂移的伪甩动。思路参考 [PowerToys Find My Mouse](https://github.com/microsoft/PowerToys/blob/cb25632f5b61eb2e57a3b0b52053409403ae9e98/src/modules/MouseUtils/FindMyMouse/FindMyMouse.cpp)，本实现沿用 ShakeSpot 的往返语义和约束。

增长和缩小阶段使用约 15 毫秒窗口计时器，停留阶段最长 100 毫秒检查一次可见性、角色和心跳。已经达到最大尺寸时，继续晃动只延长停留。移动期间立即检查角色、DPI 和边缘朝向是否发生变化，只有需要改变位图才更新；显示器边界按屏缓存，在切屏或系统通知后刷新。没有调用 `timeBeginPeriod`，计时器频率受系统调度影响。

缓存保留独立光标帧，传给 `SetSystemCursor` 的始终是副本。该 API 会消耗输入句柄，缓存对象继续拥有原件；共享系统光标句柄只借用，不释放。语义依据 [微软 SetSystemCursor 文档](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setsystemcursor)。

**内存和资源边界**

| 对象 | 约束与释放方式 |
| --- | --- |
| 识别轨迹 | 固定 96 个样本；x64 本机结构体 2,352 字节，`add` 不分配堆内存 |
| 回调队列 | 固定 64 个事件；只合并满足设备、方向和时间条件的相对移动；溢出则暂停效果 |
| 光标缓存 | 最多 16 帧，颜色与掩码数据合计预算 2 MiB；先淘汰再创建，闲置 3 秒清空 |
| 单帧 | 最大高度 256 像素，最大宽度 192；逐行完成 3 倍超采样，最终颜色位图最大 192 KiB，扫描行及覆盖计数主数组固定 1,344 字节 |
| 配置文件 | 读取上限 16 KiB；数值越界钳制；排除列表最多 32 项、输入总计 4096 字节，规则无效时拒绝保存或加载 |
| 共享状态 | 固定大小映射，仅含对齐的原子整数；不跨进程共享 Rust 指针、集合或引用 |
| Win32 资源 | 内核句柄、映射、光标、位图和注册表键按所有权释放；窗口和托盘在 App 退出时统一清理 |

2 MiB 是缓存位图数据的核算预算，不是整个进程或 Windows 内核内存上限。系统光标副本、GDI 对象管理数据、DLL、栈、分配器和临时渲染缓冲都有额外开销。没有通过强制裁剪工作集或高频 GC 制造较低读数。

`rust/lib.rs` 使用 `#![forbid(unsafe_code)]`，识别、动画、逐行栅格化和配置解析由安全 Rust 实现。FFI 集中在 `rust/win/`，启用 `unsafe_op_in_unsafe_fn = deny`，Clippy 检查 unsafe 安全注释。Windows 窗口回调只入队或读取快照，没有 `App*` 转为 `&mut App` 的路径；主循环在回调返回后处理业务，避免同步消息重入造成可变引用别名。菜单期间撤销输入注册，关闭后恢复。

所有权与固定容量降低了悬空引用、重复释放和无界累积的风险，但 Win32 FFI、系统驱动和跨进程映射仍有需要人工维护的契约。当前测试证明的是已覆盖路径的资源回收与行为一致性，不是对所有环境作零泄漏、绝不崩溃或固定总内存的承诺。OOM 或 release panic 会终止主进程，由独立恢复进程执行恢复。

**运行状态与应用排除**

用户启用开关、暂时挂起和前台应用避让分别保留。排除规则按不区分大小写的 EXE 文件名或完整路径精确匹配；程序名不会匹配带前后缀的其他名称，完整路径可区分不同安装位置。配置写为重复的 `excludedApp=` 行，旧配置没有此字段时默认为空。UI 保存前校验格式和容量，配置加载失败会明确报错。

通过 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND, WINEVENT_OUTOFCONTEXT)` 接收切换通知，不注入其他进程；读取进程路径仅申请查询权限。缓存当前窗口，移动期间不反复打开进程；触发前再核验一次。配置了排除项而暂时无法确认前台进程时，暂时停止识别。前台上下文变化会清空未完成手势，并丢弃切换前排队的原始移动；进入排除应用立即结束效果、恢复指针并撤销 Raw Input 注册，离开后按用户开关恢复。前台通知在手动暂停时仍生效，不会把用户暂停改为启用。

**恢复与兼容**

主进程必须收到恢复进程就绪信号才可修改光标。首次修改先写入并同步恢复标记，再更新共享原子状态。效果期间恢复进程每 250 毫秒检查心跳；心跳超过 1.5 秒时先结束已卡死的父进程再恢复指针，避免迟到帧覆盖恢复结果。无效果时恢复进程阻塞等待内核事件。

正常结束、点击、暂停、设置、锁屏、挂起和退出都恢复系统配置的指针方案。主进程意外退出由恢复进程恢复；恢复进程退出则主进程恢复并暂停效果。两个进程同时被杀死时，恢复标记留到下一次启动处理，也可运行 `ShakeSpot.exe --restore`。不更改系统光标主题注册表，不隐藏系统指针。跨进程共享状态依赖本项目限定的 Windows x64 对齐原子操作。

沿用 `%LOCALAPPDATA%\ShakeSpot\native-settings.ini`、`native-recovery.flag`、当前会话互斥体和原生控制协议，读取原 C++ 设置，并阻止两个原生版本同时运行。严格解析不再接受 `4junk` 这类数值尾随垃圾。Rust 错误日志为同目录 `rust-error.log`，覆盖保存单条错误，不记录鼠标轨迹。配置保存使用同目录临时文件、同步落盘和替换；设置窗口在保存失败时尝试回滚启动项变更。

启动项仍是当前用户 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 的 `ShakeSpotNative`，默认关闭。若旧版本已启用启动项或移动了 EXE，请在新版本中重新保存该选项，以更新路径。`--test-mode` 禁止桌面测试修改真实启动项。

支持 `--settings`、`--pause`、`--resume`、`--preview`、`--exit`、`--restore` 与 `--data-dir <目录>`。控制命令发送给当前会话唯一实例；没有实例时，除启动设置和显式恢复外返回 2。启动/恢复失败返回 3。保留旧协议中的类名和配置文件名以便升级与独立回归测试，界面显示 ShakeSpot。

性能基准包含水平、斜向、直线、漂移、微抖动、重复零位移输入、原始输入处理和栅格化。保存优化前后同一基准源码构建的程序后，可用 `validation/compare-benchmarks.ps1 -BeforeExe <旧基准EXE> -AfterExe <新基准EXE> -OutputDirectory <结果目录>` 交替运行；脚本核对输入量、触发次数和零分配，输出各轮数据及中位数。涉及性能的修改遵循仓库根目录 AGENTS.md 的验收要求。

主要入口：`rust/core.rs`、`rust/input.rs`、`rust/exclusions.rs`、`rust/win/foreground.rs`、`rust/raster.rs`、`rust/settings.rs`、`rust/win/app.rs`、`rust/win/ui.rs`、`rust/win/cursor.rs`、`rust/win/guardian.rs`、`scripts/rust.ps1`。实测及支持边界见 [RUST-VALIDATION.md](RUST-VALIDATION.md)。
