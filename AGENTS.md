# ShakeSpot 项目约定

当前产品为 `rust/` 中的 Rust + Win32 实现；`native/` 和 `src/` 保留历史实现，独立桌面验证驱动在 `validation/`。构建、测试和发布统一使用 `scripts/rust.ps1`。

用户明确要求持续关注性能占用。性能与识别准确性、触发延迟、指针恢复一起验收；功能完成后仍需交代资源变化。最近已发布版本的数据见 [RUST-VALIDATION.md](RUST-VALIDATION.md)，实现约束见 [RUST.md](RUST.md)。

**实现要求**

- 静止且无效果时保持消息驱动，避免周期位置轮询和无必要的高频计时器。保留必要的恢复保障与一次性缓存清理。
- 原始输入、手势判定等高频路径保持有界存储，保留零堆分配目标；避免逐输入包打开进程、读写文件、写日志或重建图形资源。前台进程信息按状态变化查询并缓存，触发前保留必要核验。
- 保留输入节流和有效反向证据，性能优化不得通过丢失手势、增加误触或削弱指针恢复换取读数改善。
- 复用光标帧，仅在尺寸、角色或朝向变化时替换系统光标；停留阶段降低计时频率。当前缓存最多 16 帧、核算位图数据最多 2 MiB，闲置 3 秒释放；调整这些边界须给出测量与行为依据。
- 借鉴 PowerToys 时评估具体实现与 ShakeSpot 的适配性。引入渲染框架、全屏合成层或常驻线程前，验证其 CPU、GPU、内存和唤醒成本。

**验证与交付要求**

- 修改输入、识别或栅格化热路径时，运行 `scripts/rust.ps1 bench`，记录耗时、样本/输入包数量和分配次数，并完成相应功能测试。保留修改前数据用于对照。
- 修改输入注册、状态处理、动画、光标或资源生命周期时，运行适用的 `scripts/rust.ps1 desktop-checks`。运行前说明会移动鼠标并操作自建窗口；使用独立测试配置，结束后恢复现场。
- 同时记录主进程和恢复进程的 CPU 时间、工作集、私有提交量，以及输入采样、动画检查和光标更新次数。重复触发并清理后核对 GDI、USER 对象和内核句柄是否回到基线。
- 关注启动后静止、设置关闭后静止、效果停留、持续甩动、暂停和应用排除场景。排除列表启用时核对监听撤销、窗口切换查询次数与恢复行为；现有脚本未覆盖的指标需补充测量或明确注明。
- 有疑似性能退化时，先按相同环境、构建和场景复测，确认后解释功能收益与资源代价。工作集与私有提交量分别报告；区分微基准和整机实测，零 CPU 计数增量不代表零开销，合成 8,000 Hz 输入不等于物理硬件验收。
- 在验证记录中保存环境、版本、场景和数据，不覆盖唯一的旧结果；交付说明包含性能结果或明确的未测项。纯文档变更无需重跑桌面或性能测试。
