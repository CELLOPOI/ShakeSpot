# 第一版验证记录

验证日期：2026-09-19。环境为 Windows 11 x64，系统版本 10.0.26100，12 个逻辑处理器，一块 3840 × 2160 显示器，175% 缩放。使用项目内 .NET SDK 10.0.401，Release 配置。未申请管理员执行级别；自动桌面验证在允许操作当前用户桌面的会话内运行。

**构建和自动化逻辑测试**

- Release 构建成功，0 警告、0 错误。
- 29/29 个逻辑测试通过，含有效晃动、普通移动、快速单向移动、小幅抖动、重复触发及边缘热点等。
- Windows x64 自包含单文件发布成功，EXE 约 165.0 MiB，包含 .NET 10.0.12 桌面运行时。内部程序集不压缩，ZIP 用于压缩分发。
- 最终发布 EXE 单独启动检查通过：约 4.15 秒内采样 249 次，退出码 0；工作集 69.7 MiB、私有内存 18.6 MiB、启动及运行累计 CPU 时间 0.25 秒。结果见 `artifacts/published-smoke.json`。

**真实 Windows 桌面测试**

38 项检查通过。使用独立进程的输入接收窗口，覆盖层在另一个进程中绘制；测试不仅检查样式位，还通过 SendInput 验证下方窗口实际收到输入。

- 不透明箭身区域的点击穿透、滚轮穿透、拖拽消息传递；显示覆盖层与操作后，前台窗口仍是输入接收窗口。
- 原生窗口位置与热点一致，支持负坐标；完整应用在真实 Windows 光标采样下触发，停止晃动后自动恢复。
- 拖拽期间抑制触发，暂停立即撤下效果，暂停时不响应晃动，恢复后接受新轨迹。
- 输入接收窗口主动隐藏光标时，程序抑制放大效果。测试窗口成对恢复显示计数；后续查询重新初始化 CURSORINFO，确认光标可见。
- 设置窗口可打开；用 Windows UI Automation 修改三项参数并点击保存，核对实际 JSON；恢复默认并保存后再次核对。关闭设置后程序继续驻留。
- 检查截图并修正了“1.1 秒”被错误显示为“11 秒”的数字格式。
- 正常退出回收覆盖层，鼠标保持可见。效果显示时强制结束应用进程，覆盖层被系统回收，鼠标仍可见，测试窗口继续收到点击。

原始结果保存于 `artifacts/desktop-checks/report.json`，应用计数保存于同目录的 `app-diagnostics.json`。最终通过运行共约 51.8 秒，采样 2510 次，触发 3 次，显示 821 帧。该运行包含暂停、UI 自动化、静止测量及效果预览，不应将全程采样次数除以总时间当作启用时的帧率。

**资源测量**

用 Win32 `GetProcessTimes` 读取被测应用进程的 CPU 时间；工作集和私有内存通过进程计数读取。每阶段约 10.3 秒，表中为阶段内采样到的工作集峰值。测试辅助进程的占用不计入。

| 场景 | 工作集峰值 | 私有内存 | CPU 观察 |
| --- | ---: | ---: | --- |
| 托盘启用、鼠标静止、尚未打开设置 | 69.1 MiB | 17.4 MiB | 最终一轮未记录到计数增量；前一轮约占一个核心 0.15% |
| 打开、自动化操作并关闭设置后，鼠标静止 | 166.2 MiB | 99.0 MiB | 10.3 秒内未记录到计数增量 |
| 指针静止，每 0.5 秒刷新一次放大效果 | 159.1 MiB | 99.4 MiB | 10.3 秒内未记录到计数增量 |
| 暂停 | 159.1 MiB | 99.3 MiB | 10.3 秒内未记录到计数增量 |

本机短测中 CPU 时间常以约 15.625 毫秒为步长，测得增量为零不能解释为完全不消耗 CPU。全程累计 CPU 时间为 1.84375 秒，含启动、首次 WPF 设置窗口加载、UI 自动化、动画及退出。工作集受 WPF、字体、UI 自动化提供程序和系统回收影响，关闭设置不会立即回到启动时的内存。持续快速移动与其他硬件下的性能尚未做长期压力测量，因此不作“零占用”或全场景低占用承诺。

发布设置经过实际对比：开启单文件程序集压缩时，启动检查的工作集约 203.3 MiB、私有内存约 93.9 MiB；关闭后分别约 69.7 MiB、18.6 MiB。因此最终包关闭内部压缩，保留 ZIP 分发。微软也说明压缩程序集需要在启动时解压到内存，并建议测量其开销，见 [单文件压缩文档](https://learn.microsoft.com/en-us/dotnet/core/deploying/single-file/overview#compress-assemblies-in-single-file-apps)。

**仍需手动验收**

- 两块及以上物理显示器、混合 DPI、跨屏切换、显示器热插拔、不同屏幕布局。单屏负坐标 HWND 检查不等同于多屏实测。
- 物理鼠标的触发手感、不同鼠标 DPI 和回报率、白色/彩色/大号系统光标的重影程度。
- 屏幕四角实际移动时的箭身翻转观感，以及跨显示器接缝时的朝向切换。边缘布局和热点逻辑已自动测试，硬件观感仍需确认。
- 开机启动项启用后的实际重新登录；测试使用隔离配置，没有写入真实用户的启动项。
- 锁屏/睡眠/远程桌面重新连接，系统托盘在 Explorer 重启后的恢复。
- 独占全屏游戏不在第一版支持范围；使用前暂停。

以上待验证项没有按“构建通过”视为通过。README 提供逐步的人工验收方法。
