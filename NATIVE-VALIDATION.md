> 历史记录：本文描述 0.2.0 C++ 版本。当前产品已迁移到 Rust，请从 README.md 和 RUST.md 开始。

# 原生版验证记录

日期：2026-09-19。Windows 11 x64，系统版本 10.0.26100，12 个逻辑处理器，一块 3840 × 2160 显示器，175% 缩放（168 DPI）。MSVC 14.44.35207、Windows SDK 10.0.26100.0，C++20 Release，`/O2 /MT /W4 /WX`。EXE 清单使用普通用户权限和 PerMonitorV2。

**已完成的验证**

- 构建成功，启用警告视为错误。原生 EXE 为 380,416 字节，约 371.5 KiB。发布不含 .NET 运行时。
- 最终发布 EXE 单独启动检查通过：恢复进程使用同一发布文件，暂停/恢复启用正常，两个进程都正常退出，主进程退出码为 0。结果见 `artifacts/native/published-smoke.json`。依赖检查只发现 Windows 自带 DLL，没有 VC++ 或 .NET 外部运行时 DLL。
- 25/25 项自动化逻辑测试通过：水平、垂直、斜向晃动，普通移动、快速单向移动、小抖动、缓慢往返、大范围扫动、拖拽抑制、重复触发、冷却、采样中断、不同采样率、灵敏度、动画连续性及边缘滞回。
- 47/47 项 Windows 桌面检查通过。实际运行的是独立应用进程和恢复进程；输入接收窗口属于另一个测试进程。通过 SendInput 产生移动、点击、滚轮和拖拽，核对真实收到的输入消息和前台窗口。
- 已确认当时可见的标准系统光标位图实际放大为 184 × 246 像素，热点为 (2, 2)。原普通箭头位图为 48 × 48 像素。效果结束后比较原普通箭头位图像素哈希、尺寸和热点，恢复一致。
- 水平和垂直晃动能通过真实 Raw Input 路径触发；停止后自动恢复。暂停撤销输入注册并恢复，重新启用接受新轨迹。
- 效果期间滚轮、点击和拖拽进入目标应用，前台焦点保持；点击开始后收起放大效果。应用隐藏光标时抑制效果；应用私有光标保持原样。
- 实屏右下角检查通过：实际系统光标位图镜像后，热点随图像镜像，真实点击坐标没有被移动。这是单屏边缘验证，不代表混合 DPI 多屏通过。
- 原生设置窗口打开、修改三项参数、保存、读取配置、恢复默认均通过；开机启动控件在测试模式中禁用，未修改真实用户的启动项。窗口截图已查看，文字和数值完整。
- 效果期间正常退出，主程序和恢复进程均结束，光标恢复。
- 效果期间强制终止主程序，恢复进程恢复光标后结束；随后目标应用仍收到点击。
- 效果期间模拟主线程卡死，恢复进程结束自己的父进程并恢复光标。
- 单独结束恢复进程，主程序恢复光标并禁用效果。
- 同时结束两个测试进程，恢复标记保留；下次启动读取标记并恢复光标，删除标记。该用例说明进程树全部被结束时必须有后续恢复动作。

原始报告为 `artifacts/native/desktop-checks/report.txt`。`actual-system-cursor.bmp` 是通过当前光标句柄读取并绘制的真实系统光标，未拍摄其他应用内容；`settings.bmp` 只包含自建设置窗口。

**资源测量**

以下为最终版本的一轮测量，各阶段约 10 秒。CPU 通过 `GetProcessTimes` 读取主程序与恢复进程的累计时间之和。表中内存为两个进程合计、阶段内采样峰值，不计测试辅助进程；工作集包含共享 DLL 页面，两进程相加可能重复计入共享页面，不能当作额外物理内存的精确值。私有内存为 PrivateUsage，表示私有提交量。

| 场景 | 合计工作集峰值 | 合计私有内存峰值 | 两进程 CPU 时间增量 |
| --- | ---: | ---: | ---: |
| 打开并关闭设置后，鼠标静止 | 26.71 MiB | 4.63 MiB | 未记录到计数增量 |
| 鼠标静止，每 450 毫秒延长一次效果 | 26.81 MiB | 4.76 MiB | 0.015625 秒，约一个核心的 0.16% |
| 持续合成晃动 | 26.83 MiB | 4.82 MiB | 未记录到计数增量 |
| 暂停 | 26.81 MiB | 4.82 MiB | 未记录到计数增量 |

静止阶段记录了 0 个 Raw Input 事件、0 次位置采样、0 次动画更新。停留效果阶段约 648 次计时器回调，但仅更新系统光标 11 次。连续晃动阶段在 10.006 秒内收到 643 个输入事件，采样 643 次，动画回调 623 次，更新指针位图 12 次；持续晃动多数时间停留在最大倍率，所以无需每帧换位图。

启动后、未打开设置的观察段合计工作集峰值为 23.17 MiB，私有内存为 4.39 MiB。但该段实际收到了 131 个外部输入事件，并触发了效果，因此不能把它列为纯静止 CPU 测量。

本机短测中的 CPU 时间常以约 15.625 毫秒为步长。记录为零不代表完全不耗 CPU，10 秒样本也不足以承诺长期表现。此前开发轮次的同类动态场景观察到约一个核心的 0.16%–0.63%，用于说明短测波动，不作为最终版本的稳定上限。最终报告没有测量 GPU 占用、功耗或内核光标资源的全部开销。

旧 WPF 版本的启动检查约 69.7 MiB 工作集、18.6 MiB 私有内存；打开设置及 UI Automation 后约 166.2 MiB / 99.0 MiB，见 `VALIDATION.md`。这些是同一机器上的不同测试轮次，UI 自动化技术也不同，可用于判断量级，不能当作严格控制变量的框架基准。没有 Rust 同功能实现，所以没有 C++ 与 Rust 的性能对比数据。

**待验证与支持边界**

- 物理鼠标手感，1000 / 8000 Hz 回报率，高速往返和长时间使用。当前连续晃动测试输入约 64 Hz，不能替代高回报率硬件压测。
- 两块及以上物理显示器、混合缩放、负坐标屏、跨屏接缝、热插拔、不同显卡的光标尺寸支持。
- 辅助功能彩色或特大指针、第三方光标主题的外观与恢复。普通当前主题的像素恢复已测，不能外推至全部主题。
- 锁屏、睡眠、远程桌面重连、Explorer 重启后的托盘恢复和开机启动后的实际重新登录。
- 独占全屏游戏、安全桌面不支持；运行游戏前暂停。应用私有光标暂不放大。
- 尚无安装器、代码签名或自动更新。原生 API 并不保证所有驱动都用硬件光标平面呈现 256 像素光标。

构建通过、逻辑测试通过和实际桌面验证在本记录中分开列出。未验证项没有按已完成报告。

