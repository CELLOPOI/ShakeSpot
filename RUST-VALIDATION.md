# Rust 版本验证记录

日期：2026-09-19。Windows 11 x64，10.0.26100，12 个逻辑处理器；一块 3840 × 2160 显示器，175% 缩放（168 DPI）。Rust/Cargo 1.93.1，MSVC 14.44、Windows SDK 10.0.26100.0。版本 0.4.1，Release、ThinLTO、静态 CRT、PerMonitorV2。

**改动与行为验证**

参考 PowerToys 减少重复完整判定的思路，新增窗口路程与净位移的必要条件预筛。每个样本只需增减累计的 L1 路程；不可能满足路程、速度或净位移要求时，跳过完整二维扫描。候选轨迹仍使用 0.4.0 的完整算法，保留其采样时机、有效反向、幅度、时间窗、冷却与恢复行为。实现及上下界推导见 [RUST.md](RUST.md)。

- 54 项 Rust 测试通过：原有 51 项和新增 3 项差分回归。
- 差分回归冻结 0.4.0 判定，完成 1,161,102 次逐样本比较，触发结果与触发样本全部一致。覆盖各档灵敏度、不同采样间隔、旋转轨迹、临界幅度、长期无触发移动、窗口淘汰、按键阻断、时间回退、长间断、坐标跳变与非法输入。
- 格式检查及 Clippy 警告视为错误的检查通过。
- 修改前、修改后分别完成 86 项桌面检查，全部通过。覆盖水平/垂直/斜向输入、屏幕位置固定时的相对甩动、应用排除与切换、手动暂停、设置保存、点击/滚轮/拖拽、光标角色与边缘方向，以及各类退出和异常恢复。
- 16 轮效果及缓存释放后，新旧版本均为 GDI 15 → 15、USER 16 → 16、内核句柄 196 → 196；缓存帧数与核算字节数归零。
- 新版突发压力检查收到 8,192 条 Raw Input，128 次移动采样，约 3.090 秒完成，程序保持启用。
- 最终发布路径独立启动检查通过：恢复进程使用相同 EXE，暂停与恢复启用正常，主进程及恢复进程均退出，主进程退出码为 0。

桌面自动化使用独立配置和自建窗口，结束后恢复指针与现场。额外编译运行了 `validation/desktop-tests.cpp` 的 `--measure-only` 模式，针对静止效果和持续晃动复测；两版均正常恢复光标。

**同机微基准对照**

本轮先扩充基准场景，再分别保存优化前后的基准 EXE，使用同一基准源码和构建配置。每个识别场景预热 16,384 次，然后测量 500,000 个样本；原始输入处理测量 2,000,000 包，栅格器测量 1,000 帧。每组交替运行新旧版本 7 轮，共做两组，以下为各版本合并 14 轮的中位数：

| 场景 | 0.4.0 | 0.4.1 | 耗时变化 |
| --- | ---: | ---: | ---: |
| 水平有效甩动 | 84.29 ns/样本 | 87.65 ns/样本 | +3.98% |
| 斜向有效甩动 | 113.35 ns/样本 | 114.02 ns/样本 | +0.59% |
| 单向直线移动 | 247.46 ns/样本 | 10.88 ns/样本 | -95.60% |
| 持续漂移并夹杂摆动 | 239.46 ns/样本 | 12.40 ns/样本 | -94.82% |
| 小幅微抖动 | 397.44 ns/样本 | 11.06 ns/样本 | -97.22% |
| 重复零位移输入 | 156.07 ns/样本 | 9.64 ns/样本 | -93.83% |
| 模拟 8,000 Hz 的有效甩动输入处理 | 6.37 ns/包 | 6.45 ns/包 | +1.18% |
| 256 像素高的栅格化帧 | 162.29 μs/帧 | 171.63 μs/帧 | +5.76% |

所有轮次的 Rust 热路径堆分配均为 0，输入量、识别样本量及触发次数保持一致。识别器从 2,344 增至 2,352 字节，输入跟踪器从 2,456 增至 2,464 字节，均只增加 8 字节累计状态。

收益集中在日常不应触发的输入。有效水平甩动存在约 4% 的微基准耗时增加，换取大量无效输入下更少的计算；两组水平场景分别为 +5.07% 和 +4.40%。触发时刻没有变化。

栅格器源码没有改动，但它作为对照也出现耗时波动：首组中位数 +9.21%，复测为 +1.05%；两组完整原始数据均保留，不能把合并变化简单归因于预筛。该微基准不含 Win32 消息、GDI 资源创建、系统光标替换和调度成本。重复零位移输入不是程序空闲场景；94%～97% 的局部计算降幅不等于整程序 CPU 或功耗下降同样比例。

**桌面 CPU 与内存**

每阶段约 10 秒。内存是主进程与恢复进程合计的阶段峰值，不计测试驱动；工作集可能重复计算共享页，私有提交量不是独占物理内存。以下来自完整桌面回归：

| 场景 | 工作集 MiB，前 → 后 | 私有提交量 MiB，前 → 后 | CPU 时间秒，前 → 后 |
| --- | ---: | ---: | ---: |
| 启动后静止 | 22.391 → 22.395 | 4.039 → 4.039 | 0.000 → 0.000 |
| 设置关闭后静止 | 26.559 → 26.641 | 4.941 → 4.996 | 0.000 → 0.000 |
| 静止并每 450 ms 延长效果 | 26.473 → 26.555 | 4.789 → 4.840 | 0.031 → 0.094 |
| 持续合成晃动 | 26.535 → 26.562 | 4.855 → 4.879 | 0.031 → 0.031 |
| 暂停 | 26.215 → 26.262 | 4.461 → 4.508 | 0.000 → 0.000 |

静止与暂停阶段均没有输入、移动采样、光标更新和动画检查。持续晃动新旧版均约占单个逻辑核的 0.31%。停留效果的高读数属于待复核波动，因此增加了相同条件的独立效果阶段测量：

| 独立效果复测 | CPU 时间秒，前 → 后 | 单逻辑核占比，前 → 后 |
| --- | ---: | ---: |
| 静止延长效果 | 0.031 → 0.016 | 0.312% → 0.156% |
| 持续合成晃动 | 0.062 → 0.047 | 0.625% → 0.468% |

复测未重现静止效果的高读数；该阶段两版均为 0 次 Raw Input 和手势采样。独立复测未先打开设置，不能把其内存峰值与完整回归的设置后阶段混为一谈。CPU 计数有约 15.625 毫秒的粒度，短测中的零值或少量计数差不能支持精确的整程序提升百分比。本轮证据支持“无效轨迹计算显著减少，程序整体资源仍处于原有量级”，不支持整体 CPU、GPU 或功耗显著下降的结论；GPU 与功耗未测。

**数据位置与复现**

维护者本机的完整资料保存在 `artifacts/performance-review/20260919-215356/`；这些构建与测试产物不纳入 Git 仓库，克隆仓库不会包含此目录。可复核的汇总数据已记录在本文，复现入口随源码提供。原始资料包括：

- `before/`：优化前源码、EXE、基准 EXE、基准输出、完整桌面报告与独立效果报告。
- `after/`：优化后基准 EXE、基准输出、完整桌面报告与独立效果报告。
- `paired/`、`paired-repeat/`：两组各 7 轮的输出、文件哈希、逐轮读数和各组中位数。
- `aggregate.json`：合并 14 轮中位数；未删除或覆盖第一组波动较大的结果。

常规入口为 `scripts/rust.ps1 test`、`check`、`bench` 和 `desktop-checks`。交替比较使用 `validation/compare-benchmarks.ps1 -BeforeExe <旧基准EXE> -AfterExe <新基准EXE> -OutputDirectory <结果目录>`。已经构建的桌面驱动可以用 `ShakeSpot.DesktopTests.exe <待测ShakeSpot.exe> --measure-only` 单独测量效果阶段；该模式仍会操作鼠标和自建窗口。

0.4.0 的历史验证报告保存在 `artifacts/rust/VALIDATION-0.4.0.md`；0.3.0 历史报告也继续保留。产品实现与资源约束见 [RUST.md](RUST.md)，项目验收要求记录在仓库根目录的 AGENTS.md。

**发布文件与边界**

最终 EXE 为 425,472 字节（415.5 KiB），SHA-256：`F8E5B57C2A8CEA55DFADA2EC3AF2D04FA32CDDEBFDFAB038CB76FF5C8F97ACB0`。桌面回归、发布启动检查与本机发布文件的哈希一致。此哈希对应上述工具链构建的 EXE；其他机器或工具链的产物以各自的 BUILD-INFO.txt 和 SHA256SUMS.txt 为准。

程序为 `artifacts/publish/rust-win-x64/ShakeSpot.exe`，压缩包为 `artifacts/ShakeSpot-0.4.1-rust-win-x64.zip`；启动检查记录为 `artifacts/rust/published-smoke.json`。版本未引入新的运行时依赖。

物理高回报率鼠标、不同硬件 DPI 手感、长期运行、混合 DPI 多屏、远程桌面、锁屏/睡眠与真实重新登录启动仍需对应环境验收。合成输入与短时资源检查不能替代这些验证。整个进程树同时结束后，仍需下次启动或显式 `--restore` 恢复指针。
